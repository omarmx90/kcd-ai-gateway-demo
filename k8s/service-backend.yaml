apiVersion: v1
kind: Service
metadata:
  name: ai-backend
  namespace: ai-gateway-demo
  labels:
    app: ai-backend
spec:
  selector:
    app: ai-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
