apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-backend
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: ai-backend
  namespaceSelector:
    matchNames:
      - ai-gateway-demo
  endpoints:
    - port: http
      path: /metrics
      interval: 10s
